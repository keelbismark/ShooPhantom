# Contributing to ShooPhantom

Спасибо за интерес к развитию проекта! Это руководство поможет вам начать.

## Как начать

### Предварительные требования

- Java 21
- Maven 3.8+
- JDK 21 (Temurin/OpenJDK)
- Git

### Настройка окружения

1. Fork репозитория и клонируйте его:
```bash
git clone https://github.com/YOUR_USERNAME/ShooPhantom.git
cd ShooPhantom
```

2. Соберите проект:
```bash
mvn clean install
```

3. Убедитесь, что тесты проходят:
```bash
mvn test
```

## Разработка

### Структура кода

- **Records**: Используйте Java records для неизменяемых данных (например, `Ward`)
- **Pattern Matching**: Используйте современный синтаксис Java 21
- **Dependency Injection**: Избегайте прямых вызовов `new`, вместо этого внедряйте зависимости через конструктор
- **Exceptions**: Используйте исключения для обработки ошибок

### Стандарты кода

Проект использует Checkstyle для обеспечения консистентности кода. Запустите проверку:
```bash
mvn checkstyle:check
```

Основные правила:
- Максимальная длина строки: 120 символов
- Отступы: 4 пробела
- Имена: camelCase для переменных/методов, PascalCase для классов, UPPER_SNAKE_CASE для констант
- Javadoc: Обязателен для public методов

### Тестирование

Все новые функции должны включать unit тесты.

#### Написание тестов

Используйте JUnit 5 и Mockito:
```java
@DisplayName("Should do something when condition")
@Test
void testSomething() {
    // Arrange
    Mock mock = mock(Mock.class);
    when(mock.someMethod()).thenReturn(expected);
    
    // Act
    Result result = someService.doSomething();
    
    // Assert
    assertEquals(expected, result);
}
```

#### Запуск тестов

```bash
mvn test
```

#### Покрытие кода

```bash
mvn jacoco:report
```

Отчет будет в `target/site/jacoco/index.html`

### Commit Messages

Используйте конвенцию Conventional Commits:
- `feat: add tier 4 protection` - новая функция
- `fix: resolve NPE in fuel manager` - исправление бага
- `docs: update README` - документация
- `test: add tests for Ward` - тесты
- `refactor: simplify configuration loading` - рефакторинг
- `style: add missing Javadoc` - стиль кода

### Pull Request

Перед созданием PR:
1. Обновите вашу ветку с main:
```bash
git checkout main
git pull upstream main
git checkout feature/my-feature
git rebase main
```

2. Запустите все проверки:
```bash
mvn clean test checkstyle:check
```

3. Убедитесь, что все тесты проходят и нет ошибок Checkstyle

4. Push и создайте PR с информативным описанием включающим:
   - Описание изменений
   - Связанные issues
   - Скриншоты (если применим UI)
   - Инструкции по тестированию

## Code Review

При review используйте следующие критерии:

### ✅ Должно быть
- Есть unit тесты для новых функций
- Тесты покрывают edge cases
- Код соответствует стилю (Checkstyle проверку проходит)
- Javadoc для public API
- Нет warnings в IDE
- Нет `TODO`, `FIXME` в производственном коде

### ❌ Не должно быть
- `System.out.println()` - используйте logger
- Игнорируемые исключения без комментария почему
- Закомментированный код
- Magic numbers - выносите в константы или config
- Дублирование кода (DRY principle)

## Задачи

Для доступа к задачам и обсуждениям:
- [Issues](https://github.com/keelbismark/ShooPhantom/issues) - баги и feature requests
- [Discussions](https://github.com/keelbismark/ShooPhantom/discussions) - вопросы и идеи

## Релизы

Релизы следуют семантическому версионированию:
- MAJOR.MINOR.PATCH
- MAJOR: несовместимые изменения API
- MINOR: новая функциональность (backwards compatible)
- PATCH: исправления багов (backwards compatible)

## Лицензия

Вкладывая в проект, вы соглашаетесь, что ваш вклад будет лицензирован под MIT License.

## Связь

Если у вас есть вопросы:
- Откройте Discussion
- Создайте Issue с вопросом
- Напишите maintainer'у

Спасибо за ваш вклад! 🙏
